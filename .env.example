# Copia a .env y rellena (la extensión telegram-notify lo relee en cada uso).
# Alternativa: `/telegram set <bot_token> [chat_id]` lo rellena solo.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# 1 = notificar al terminar, 0 = silenciar
TELEGRAM_NOTIFY=1
